"use client";
import { motion } from "framer-motion";
import { ArrowLeft, ArrowRight, Star } from "lucide-react";
import { useEffect, useState } from "react";
const testimonials = Array.from({length:8},(_,i) => ({name:"TODO · Nombre cliente", role:"TODO · Empresa / cargo", quote:"TODO · Añade aquí un testimonio real que refleje la experiencia con Premium Work.", id:i}));
export function Testimonials() { const [page,setPage]=useState(0); const [hovered,setHovered]=useState(false); const visible = testimonials.slice(page * 4, page * 4 + 4); const go=(v:number) => setPage(p=>(p+v+2)%2); useEffect(()=>{if(hovered)return;const id=window.setInterval(()=>go(1),5600);return()=>window.clearInterval(id)},[hovered]); return <section className="section-pad bg-[#F8F7F4]"><div className="mx-auto max-w-[1600px]"><motion.div initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:"-100px"}} className="grid gap-8 border-t border-[#0B1F3A]/15 pt-10 lg:grid-cols-2"><p className="eyebrow">◆ Testimonios de clientes</p><h2 className="display text-5xl leading-[.98] md:text-7xl">Lo que se siente cuando todo está cuidado.</h2></motion.div><div onMouseEnter={()=>setHovered(true)} onMouseLeave={()=>setHovered(false)} className="mt-16 grid border border-[#0B1F3A]/15 sm:grid-cols-2 lg:grid-cols-4">{visible.map((t,i)=><motion.figure key={t.id} initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:i*.05}} className="flex min-h-80 flex-col border-b border-[#0B1F3A]/15 p-6 last:border-b-0 sm:[&:nth-child(odd)]:border-r lg:border-b-0 lg:border-r lg:last:border-r-0"><figcaption><p className="font-bold">{t.name}</p><p className="mt-1 text-sm text-[#0B1F3A]/65">{t.role}</p><div className="mt-5 flex text-[#C9A227]">{Array.from({length:5},(_,x)=><Star key={x} size={17} fill="currentColor"/>)}</div></figcaption><blockquote className="mt-10 text-base leading-7 text-[#0B1F3A]/80">“{t.quote}”</blockquote></motion.figure>)}</div><div className="flex"><button aria-label="Testimonios anteriores" onClick={()=>go(-1)} className="border border-t-0 border-[#0B1F3A]/30 p-4"><ArrowLeft size={18}/></button><button aria-label="Testimonios siguientes" onClick={()=>go(1)} className="border border-l-0 border-t-0 border-[#0B1F3A]/30 p-4"><ArrowRight size={18}/></button></div></div></section>; }
