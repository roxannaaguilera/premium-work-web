"use client";
import { motion } from "framer-motion";
const logos = Array.from({ length: 8 }, (_, index) => `TODO · Cliente ${String(index + 1).padStart(2, "0")}`);
export function ClientsCarousel() { const loop = [...logos, ...logos]; return <section className="section-pad overflow-hidden bg-[#F8F7F4]"><div className="mx-auto max-w-[1600px]"><motion.div initial={{ opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, margin: "-100px" }} className="grid gap-8 lg:grid-cols-2"><p className="eyebrow">◆ Empresas que confían en nosotros</p><h2 className="display max-w-4xl text-5xl leading-[.98] md:text-7xl">Relaciones que se construyen servicio a servicio.</h2></motion.div></div><div className="group mt-16 overflow-hidden border-y border-[#0B1F3A]/15 py-7"><div className="marquee-track flex w-max group-hover:[animation-play-state:paused]">{loop.map((logo, index) => <div key={`${logo}-${index}`} className="flex h-28 w-60 shrink-0 items-center justify-center border-r border-[#0B1F3A]/15 px-6 text-center text-xs font-bold uppercase tracking-[.14em] grayscale opacity-45">{logo}</div>)}</div></div></section>; }
